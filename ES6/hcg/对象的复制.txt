引用传递：
	let obj1 = { a: 1, b: 2};
	let b = obj1;
	b.a=222222;
	console.log(obj1);
	console.log(b);
值传递：
	let obj1 = 111;
	let b = obj1;
	b=222222;
	console.log(obj1);
	console.log(b);
浅拷贝：

深拷贝：

对象拷贝的方式：
	1. 扩展运算符
	2. Object.assign()
		Object.assign方法用于对象的合并，将源对象（source）的所有可枚举属性，复制到目标对象（target）。
		https://www.jianshu.com/p/d5f572dd3776